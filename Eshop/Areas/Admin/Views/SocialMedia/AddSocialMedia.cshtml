@using Domain.ViewModels.SocialMedia
@model AddSocialMediaLinkViewModel
@{
    ViewData["Title"] = "اضافه کردن شبکه اجتماعی جدید";
}

<div style="margin-top: 110px" class="col-lg-12 col-sm-6 col-xs-12">
    <form method="post" asp-controller="SocialMedia" asp-action="AddSocialMedia">
        <div class="widget">
            <div class="widget-header bordered-bottom bordered-pink">
                <span class="widget-caption">افزودن شبکه اجتماعی</span>
            </div>
            <div class="widget-body">
                <div id="horizontal-form">

                    <div class="row">
                        <div class="col-md-6">
                            <select asp-for="PlatForm" asp-items="@Html.GetEnumSelectList(typeof(AddPlatForm))" placeholder="پلفترم" class="form-control"></select>
                            <div class="horizontal-space"></div>
                            <span class="danger" asp-validation-for="PlatForm"></span>
                        </div>
                        <div class="col-md-6">
                            <input type="text" asp-for="Url" placeholder="لینک" class="form-control">
                            <div class="horizontal-space"></div>
                        </div>
                    </div>
                    <div class="horizontal-space"></div>

                    <button type="submit" class="btn btn-palegreen btn-lg shiny">ثبت</button>
                </div>

            </div>

        </div>
    </form>
</div>
